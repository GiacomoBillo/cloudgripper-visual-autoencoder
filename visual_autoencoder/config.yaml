model:
  name: "ConvolutionalEncoder"
  input_channels: 3
  dimensions_to_learn: [0] # x

training:
  batch_size: 32
  epochs: 100
  validation: true
  early_stopping: false
  optimizer: "Adam" # "Adam", "SGD"
  learning_rate: 0.001 # null -> default
  
data:
  dataset_path: "D:\\Jack\\KTH\\Research project in Robotics\\cloudgripper-visual-autoencoder\\autograsper\\recorded_data\\toy_dataset_xz"
  top_img_shape: [720, 1280]
  resize_factor: 16
  split: [8000, 2000, 300] # train, val, test split of the dataset
  # num/ratio of samples to effectively use from the training set
  # if null, use all samples
  train_fraction_used: 32 # null
  # num or ratio of samples to effectively use from the validation set
  # if null, use all samples
  val_fraction_used: 32 # null
  images_to_load: ["Images"]

